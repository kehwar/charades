export default defineAppConfig({
    /* Tailwind */
    ui: {
        primary: "lime",
        gray: "neutral",
        card: {
            base: "flex flex-col",
            body: {
                base: "h-full overflow-auto",
            },
        },
    },

    /* Tailwind */
});
